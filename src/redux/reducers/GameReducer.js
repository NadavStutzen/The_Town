const initialState = {
    date: '',
    timeDuration: '',
    killer: '',
    winner: '',
};

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
