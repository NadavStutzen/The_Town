const initialState = {
    name: '',
    id: '',
    users: [],
    games: [],
};

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
