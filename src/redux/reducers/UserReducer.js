const initialState = {
    id: '',
    email: '',
    name: '',
    towns: [],
};

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
